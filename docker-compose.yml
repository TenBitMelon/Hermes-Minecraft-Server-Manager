version: '3'
services:
  app:
    image: "hermes-mcsm-website-image"
    container_name: "hermes-mcsm-website"
    ports:
      # Website external port (feel free to change): internal port
      - "3100:3000"
    restart: unless-stopped